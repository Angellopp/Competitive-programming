#include <bits/stdc++.h>
using namespace std;
const int MAXN = 3*1e5;
int main() {
    int tt, n;
    cin >> tt;
    int a[MAXN];
    while (tt--) {
        cin >> n;
        for (int i = 0; i < n; i++) cin >> a[i];
        int cnt = 2;
        for (int i = 2; i < n; i++) {
            
        }
    }
}