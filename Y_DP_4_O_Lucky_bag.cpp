#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main () {
    int n, k;
    cin >> n >> k;
    vector <int> v(n);
    for (int i = 0; i < n; i++) cin >> v[i];
    ll c = 0;
    for (int i = 0; i < n; i++) c+=v[i];
    sort(v.begin(), v.end());
    for (int i = 0; i < n; i+=) {

    }
}